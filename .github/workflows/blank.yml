name: hello world 3 jobs
on: push
jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: |
          echo "a demo build"
          echo "Your database password is: ${{secrets.DB_PW}}"
       
  
